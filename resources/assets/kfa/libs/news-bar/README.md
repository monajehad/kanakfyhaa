## ⚙️ التثبيت

### تضمين مباشر
```html
<link rel="stylesheet" href="/assets/css/news-bar.css" />
<script src="/assets/js/news-bar.v2.js"></script>
````

### عبر ES Module

```js
import NewsBar from './news-bar.v2.js';
```

---

## 🚀 التهيئة الأساسية

```js
const bar = new NewsBar({
  container: "#newsBar",
  track: "#newsTrack",
  speed: 120,            // السرعة بالبكسل/ثانية
  direction: "rtl",      // rtl | ltr
  startDelay: 1000,      // تأخير البدء بالملّي ثانية
  pauseOnHover: true,    // إيقاف مؤقت عند مرور الماوس
  theme: "light",        // light | dark | custom
  sound: true,           // تشغيل صوت عند تحديث الأخبار
  animation: "scroll"    // scroll | fade
});
```

---

## 🎨 متغيرات CSS الافتراضية

| المتغير                      | القيمة الافتراضية | الوصف                    |
| ---------------------------- | ----------------- | ------------------------ |
| `--news-bg`                  | `#c7db09`         | لون الخلفية              |
| `--news-color`               | `#000`            | لون النص                 |
| `--news-height`              | `40px`            | ارتفاع الشريط            |
| `--news-font-size`           | `1rem`            | حجم الخط                 |
| `--news-item-padding-inline` | `2rem`            | التباعد الداخلي لكل عنصر |
| `--news-item-margin-inline`  | `0.5rem`          | الهامش بين العناصر       |

---

## 🧠 الخصائص

| الخاصية        | النوع    | الافتراضي    | الوظيفة                |
| -------------- | -------- | ------------ | ---------------------- |
| `speed`        | Number   | 100          | السرعة بالبكسل / ثانية |
| `direction`    | String   | "rtl"        | اتجاه التمرير          |
| `startDelay`   | Number   | 1000         | تأخير البدء            |
| `pauseOnHover` | Boolean  | true         | إيقاف عند التمرير      |
| `theme`        | String   | "light"      | الثيم الحالي           |
| `sound`        | Boolean  | false        | تفعيل الصوت            |
| `animation`    | String   | "scroll"     | نوع الأنيميشن          |
| `container`    | Selector | `#newsBar`   | عنصر الحاوية           |
| `track`        | Selector | `#newsTrack` | عنصر المسار            |

---

## 🔧 الدوال المتاحة

| الدالة              | الوظيفة                           | مثال                              |
| ------------------- | --------------------------------- | --------------------------------- |
| `setSpeed(value)`   | تغيير السرعة أثناء التشغيل        | `bar.setSpeed(150)`               |
| `setDirection(dir)` | تغيير الاتجاه                     | `bar.setDirection("ltr")`         |
| `pause()`           | إيقاف مؤقت                        | `bar.pause()`                     |
| `resume()`          | استئناف                           | `bar.resume()`                    |
| `setTheme(theme)`   | تبديل الثيم                       | `bar.setTheme("dark")`            |
| `setItems(array)`   | تعيين قائمة أخبار جديدة           | `bar.setItems(["خبر 1","خبر 2"])` |
| `loadFrom(url)`     | تحميل الأخبار من واجهة API (JSON) | `bar.loadFrom("/api/news")`       |

---

## 🗞️ تحميل البيانات

### 1️⃣ من مصفوفة

```js
bar.setItems([
  "افتتاح مشروع جديد 🎉",
  "تحديث الإصدار القادم 🔥",
  "انضم إلينا في فعالية كأنك فيها 🎧"
]);
```

### 2️⃣ من API خارجي

```js
// يُتوقع أن تُعيد الواجهة: { items: ["نص1","نص2", ...] }
bar.loadFrom("https://api.kaannakfiha.com/news");
```

---

## 🖱️ عناصر قابلة للنقر

```html
<span data-link="https://lahhiqni.com" data-priority="breaking">
  🚨 خبر عاجل: افتتاح فرع جديد!
</span>
```

* `data-link`: يفتح الرابط في علامة تبويب جديدة
* `data-priority="breaking"`: يضيف لونًا مميزًا أو وميضًا خاصًا

---

## 🔊 الصوتيات

* تفعيل الخاصية `sound: true` يشغّل ملف `/assets/media/tick.mp3` عند وصول خبر جديد
* يمكنك تغيير الصوت بوضع ملفك الخاص في نفس المجلد

---

## 🧭 الأحداث (Events)

| الحدث        | الوصف          |
| ------------ | -------------- |
| `start`      | عند بدء الحركة |
| `pause`      | عند التوقف     |
| `resume`     | عند الاستئناف  |
| `newsChange` | عند تبدل الخبر |

### مثال

```js
bar.on("newsChange", item => {
  console.log("الخبر الحالي:", item?.textContent);
});
```

---

## 🎨 الثيمات

### Light

```css
[data-theme="light"] {
  --news-bg: #c7db09;
  --news-color: #000;
}
```

### Dark

```css
[data-theme="dark"] {
  --news-bg: #111;
  --news-color: #f9f9f9;
}
```

---

## ⚡ أنواع الأنيميشن

| القيمة     | الوصف               |
| ---------- | ------------------- |
| `"scroll"` | تمرير مستمر أفقي    |
| `"fade"`   | ظهور واختفاء تتابعي |

---

## 🧩 تكامل Laravel / React

### Laravel Blade

```blade
<div id="newsBar" class="news-bar">
  <div id="newsTrack" class="news-track"></div>
</div>
<script>
  new NewsBar({
    container: "#newsBar",
    track: "#newsTrack",
    loadFrom: "{{ route('api.news') }}",
    theme: "dark"
  });
</script>
```

### React

```jsx
useEffect(() => {
  new NewsBar({ container: "#newsBar", speed: 100, direction: "rtl" });
}, []);
```

---

## 🧱 الأداء و SSR

* يعمل حتى بدون JS (يعرض أول عنصر ثابت)
* خفيف (< 10 KB) ولا يحتاج تبعية
* يحافظ على الأداء 30 FPS +

---

## 🪪 الترخيص

**MIT License** © 2025 — Samer Abd Al Ati
مسموح بالاستخدام الحر والتعديل الكامل.

---

## 🔮 Roadmap v2.1

* كشف RTL/LTR تلقائي من HTML
* دعم Firebase Realtime Updates
* لوحة إدارة لتحرير الأخبار من Dashboard داخلي
